fastapi==0.115.0
uvicorn[standard]==0.30.0
yfinance==1.4.1
pandas>=2.3.2
pandas-ta==0.4.71b0
python-dateutil==2.9.0
requests==2.32.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.30.0
# Sync Postgres driver for Alembic: env.py converts the async URL to a sync
# postgresql:// URL (psycopg2 dialect); asyncpg is async-only and unusable here.
psycopg2-binary>=2.9.9
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Rate limiting
slowapi==0.1.9

# Structured logging
structlog==24.4.0

# In-process scheduling (nightly signal scan)
apscheduler==3.10.4

# Utilities
pydantic[email]==2.10.0
httpx==0.27.0
ta>=0.10.0
cryptography>=43.0.0

aiosmtplib==3.0.2
httpx==0.27.0
bcrypt==4.2.0
